import '../../../../core/network/api/api_consumer.dart';

class MenuRepo{
  final  ApiConsumer api ;
  MenuRepo(this.api);
  //=====================
  go() async {
    // final res = await api.get(path: 'users');
    // debugPrint('res: $res');
  }
}
